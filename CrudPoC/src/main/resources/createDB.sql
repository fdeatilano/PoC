CREATE TABLE users(
	id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	login VARCHAR(255),
	password VARCHAR(255),
	role VARCHAR(50)
);

CREATE TABLE places(
	id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	longitude DOUBLE, 
	latitude DOUBLE,
	address VARCHAR(255)
);
	
CREATE TABLE checkins(
	id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	user_id INTEGER CONSTRAINT users_id_fk FOREIGN KEY REFERENCES users(id),
	place_id INTEGER CONSTRAINT places_id_fk FOREIGN KEY REFERENCES places(id),
	check_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	longitude DOUBLE, 
	latitude DOUBLE,
	device VARCHAR(255)
);

